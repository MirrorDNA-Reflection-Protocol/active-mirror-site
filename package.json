{
  "name": "active-mirror-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "npx wrangler dev --config worker/wrangler.jsonc",
    "worker:deploy": "npx wrangler deploy --config worker/wrangler.jsonc",
    "worker:types": "npx wrangler types --config worker/wrangler.jsonc"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
